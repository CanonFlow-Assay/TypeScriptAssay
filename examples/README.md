# Examples

`clean` is a minimal policy-controlled project. It uses unknown at the transport boundary, narrows it before constructing a readonly domain value, returns an explicit discriminated outcome, and has no direct domain infrastructure effect.

Run from the repository root after building:

```text
node dist/src/cli.js verify examples/clean --json artifacts/clean-receipt.json --sarif artifacts/clean.sarif
```
